import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'card',
  imports: [RouterLink],
  templateUrl: './card.html',
})
export class Card {}
